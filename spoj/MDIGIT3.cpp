#include <cstdio>
int main()
{
    long long a,b,i;
    long long num[10];
    while(1)
    {
        scanf("%lld%lld",&a,&b);
        if(a<=0 && b<=0)
            break;
        for(i=0;i<9;i++)

    }
    return 0;
}
