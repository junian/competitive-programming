#include <cstdio>
#include <set>
#include <cstring>

using namespace std;

int main()
{
    set<char[201]> dictionary;
    char data[201];
    char inputan[201];
    char *pointer;
    char *ans;

    while(gets(data))
    {
        pointer = &(data[0]);
        ans = &(inputan[0]);
        while(*pointer!='\0')
        {
            if
    }

    return 0;
}
